structure of the /src

- controllers - these contain the controller files for the http/api
- middlewares - middlewares that will be used inside /src
- routes - these contain route definitions for websockets
- websockets - these contain route definitions for websockets